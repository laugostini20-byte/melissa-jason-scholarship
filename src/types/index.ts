export interface Recipient {
  name: string;
  year: number;
  description: string;
  photo: string;
}

export interface Event {
  title: string;
  date: string;
  description: string;
}

export interface FAQItem {
  question: string;
  answer: string;
}

